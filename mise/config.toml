# mise — global runtime/tool versions. https://mise.jdx.dev
# Project-level versions live in each repo's mise.toml / .tool-versions.

[settings]
idiomatic_version_file_enable_tools = ["node"]

[settings.ruby]
compile = false

[tools]
ruby = "4.0.5"
python = "3.14.6"
node = "26.3.1"
